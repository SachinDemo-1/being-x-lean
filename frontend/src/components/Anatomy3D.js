// Anatomy3D.js - iPhone-compatible SVG anatomy (Three.js replaced for mobile support)
// The actual 3D SVG anatomy is rendered directly in Home.js via FrontBody/BackBody components
// This component is kept as a pass-through for compatibility

import React from 'react';

export default function Anatomy3D({ onMuscleClick }) {
  // This component is superseded by the inline SVG anatomy in Home.js
  return null;
}
